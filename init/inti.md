# Purpose: Acts as developer instructions, tech stack configurations, run/test commands, and strict style rules (e.g., "keep components under 150 lines")

# Why you need it: Automatically loads context every time the AI agent opens your project workspace

# ecah time load or run \init update this file with what we have as update in this app
